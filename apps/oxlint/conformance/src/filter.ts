// Options to filter tests to a specific rule or specific code.
// Useful for debugging a particular test case.
export const FILTER_RULE: string | null = null;
export const FILTER_CODE: string | null = null;
